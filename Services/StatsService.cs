using MySql.Data.MySqlClient;
using vtb_backend.Models;

namespace vtb_backend.Services
{
    public static class StatsService
    {
        // public static bool MarkAchievementDone(int id)
        // {
        //     var dbase = new DBManager();
        //     var (hash, salt) = GenerateHash(user.Password);
        //     var cmd = new MySqlCommand(
        //         $"insert into usersachievs(user_id,achiev_id) values(@user_id,@ach_id)");
        //     cmd.Parameters.Add(new[]
        //     {
        //         "@user_id", .Name, "@ach_id", user.Email
        //     });
        // }
    }
}